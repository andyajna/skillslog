<%= form_for(@skill) do |f| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :standard %><br />
    <%= f.text_area :standard %>
  </div>
  <div class="field">
    <%= f.label :observed %><br />
    <%= f.date_select :observed %>
  </div>
  <div class="field">
    <%= f.label :performed %><br />
    <%= f.date_select :performed %>
  </div>
  <div class="field">
    <%= f.label :taught %><br />
    <%= f.date_select :taught %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
